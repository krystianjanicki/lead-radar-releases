# Stable channel

This directory is reserved for the signed Lead Radar stable update manifest.

`latest.json` is published only after a verified Authenticode-signed stable Windows release is uploaded and anonymously re-verified.
